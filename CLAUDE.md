# Laravel Swarm — Agent Context

Use `AGENTS.md` as the canonical project guidance for this repository.

This file intentionally points to `AGENTS.md` instead of duplicating the same package context. Keep project conventions, architecture notes, testing commands, and current-state guidance in one place so assistant-specific instruction files do not drift.
