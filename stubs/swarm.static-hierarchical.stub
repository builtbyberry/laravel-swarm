<?php

declare(strict_types=1);

namespace {{ namespace }};

use BuiltByBerry\LaravelSwarm\Attributes\Topology;
use BuiltByBerry\LaravelSwarm\Concerns\Runnable;
use BuiltByBerry\LaravelSwarm\Contracts\HasRoutePlan;
use BuiltByBerry\LaravelSwarm\Contracts\Swarm;
use BuiltByBerry\LaravelSwarm\Enums\Topology as TopologyEnum;

/**
 * {{ class }} — generated by `php artisan make:swarm:swarm --topology=static-hierarchical`.
 *
 * Static-hierarchical topology: the route plan is declared up front in
 * `plan()` instead of being decided by a coordinator agent at runtime. Use
 * this when the routing graph is fixed and you want the runner to follow it
 * without invoking a coordinator on every step.
 *
 * Next step: docs/static-hierarchical-topology.md
 */
#[Topology(TopologyEnum::StaticHierarchical)]
class {{ class }} implements HasRoutePlan, Swarm
{
    use Runnable;

    /**
     * @return array<int, \Laravel\Ai\Contracts\Agent>
     */
    public function agents(): array
    {
        return [
            // new \App\Ai\Agents\YourFirstAgent,
            // new \App\Ai\Agents\YourSecondAgent,
        ];
    }

    /**
     * @return array{start_at: string, nodes: array<string, array<string, mixed>>}
     */
    public function plan(): array
    {
        return [
            'start_at' => 'finish',
            'nodes' => [
                'finish' => [
                    'type' => 'finish',
                    'output' => '',
                ],
            ],
        ];
    }
}
